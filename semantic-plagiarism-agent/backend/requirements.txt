# --- Phase 1: NLP pipeline (used now) ---
pydantic>=2.6
pydantic-settings>=2.2
numpy>=1.26
PyMuPDF>=1.24
python-docx>=1.1
pytest>=8.0
pytest-cov>=5.0

# Optional, better sentence splitting (RegexSentenceSplitter is the
# zero-dependency default; install these for SpacySentenceSplitter):
# spacy>=3.7
# nltk>=3.8

# --- Phase 1: real embeddings (heavy; needs internet to fetch model weights) ---
sentence-transformers>=2.7
torch>=2.2

# --- Phase 2: vector index & retrieval ---
faiss-cpu>=1.8
scikit-learn>=1.4

# --- Phase 3: database & API ---
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
python-multipart>=0.0.9
httpx>=0.27
